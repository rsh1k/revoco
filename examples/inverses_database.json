{
  "specs": [
    {
      "arg_map": {},
      "degraded_kind": "irreversible",
      "gates": [],
      "inverse_tool": "db.noop",
      "kind": "reversible",
      "notes": "A read changes nothing, though it may still expose data it should not.",
      "one_shot": false,
      "residue": "",
      "snapshot_fields": [],
      "static_args": {},
      "steps": [],
      "tool": "db.select",
      "window_seconds": null
    },
    {
      "arg_map": {
        "key": "args.key",
        "table": "args.table",
        "values": "snapshot.prior_values"
      },
      "degraded_kind": "irreversible",
      "gates": [
        {
          "check_at": "authorize",
          "description": "Every row the statement will change must have been captured beforehand. That is only feasible for a bounded, well-targeted statement.",
          "name": "db_affected_rows_captured",
          "remediation": "Require a WHERE clause and a row-count ceiling in policy. An unbounded UPDATE or DELETE should be denied, not snapshotted."
        },
        {
          "check_at": "undo",
          "description": "The row must not have been modified by anyone else since the snapshot was taken. Writing captured values back over a concurrent edit destroys that edit.",
          "name": "db_row_unchanged_since_capture",
          "remediation": "Compare the row's current version or updated_at against the snapshot. If it moved, a human has to merge \u2014 this is not a mechanical undo."
        }
      ],
      "inverse_tool": "db.update_row",
      "kind": "compensable",
      "notes": "Capture row_version (or updated_at) alongside the values so the concurrency gate has something to compare. Without it the 'undo' is a blind overwrite, which is how a recovery becomes a second incident.",
      "one_shot": false,
      "residue": "Restoring prior values does not undo what read the row in between: triggers fired, change-data-capture streams published, downstream caches and search indexes ingested the bad value, and any decision made on it stands.",
      "snapshot_fields": [
        "prior_values",
        "row_version"
      ],
      "static_args": {},
      "steps": [],
      "tool": "db.update_row",
      "window_seconds": null
    },
    {
      "arg_map": {
        "rows": "snapshot.deleted_rows",
        "table": "args.table"
      },
      "degraded_kind": "irreversible",
      "gates": [
        {
          "check_at": "authorize",
          "description": "Every row the statement will change must have been captured beforehand. That is only feasible for a bounded, well-targeted statement.",
          "name": "db_affected_rows_captured",
          "remediation": "Require a WHERE clause and a row-count ceiling in policy. An unbounded UPDATE or DELETE should be denied, not snapshotted."
        }
      ],
      "inverse_tool": "db.insert_rows",
      "kind": "compensable",
      "notes": "Only meaningful for a bounded delete. The authorize gate is what stops an unbounded DELETE from being classified as recoverable.",
      "one_shot": false,
      "residue": "Re-inserting restores the data but not necessarily the identity: auto-increment keys may differ, sequences have advanced, and rows that referenced the deleted ones by foreign key may have cascaded away and not be covered by this capture.",
      "snapshot_fields": [
        "deleted_rows"
      ],
      "static_args": {},
      "steps": [],
      "tool": "db.delete_rows",
      "window_seconds": null
    },
    {
      "arg_map": {},
      "degraded_kind": "unknown",
      "gates": [],
      "inverse_tool": null,
      "kind": "unknown",
      "notes": "Arbitrary SQL cannot be classified, for the same reason arbitrary shell cannot. UNKNOWN escalates to a human under the starter policy, which is the right outcome \u2014 a statement that might be a SELECT and might be a DROP should not inherit either classification.",
      "one_shot": false,
      "residue": "",
      "snapshot_fields": [],
      "static_args": {},
      "steps": [],
      "tool": "db.execute_sql",
      "window_seconds": null
    },
    {
      "arg_map": {},
      "degraded_kind": "irreversible",
      "gates": [],
      "inverse_tool": null,
      "kind": "irreversible",
      "notes": "Point-in-time recovery may exist at the infrastructure layer, but restoring an entire database to undo one statement is a DBA decision under an incident process, not an automated inverse. Deny this to agents.",
      "one_shot": false,
      "residue": "",
      "snapshot_fields": [],
      "static_args": {},
      "steps": [],
      "tool": "db.drop_table",
      "window_seconds": null
    },
    {
      "arg_map": {},
      "degraded_kind": "irreversible",
      "gates": [],
      "inverse_tool": null,
      "kind": "irreversible",
      "notes": "Registered separately from delete_rows because it looks similar and behaves completely differently: no per-row capture is feasible and most engines do not even log it row by row.",
      "one_shot": false,
      "residue": "",
      "snapshot_fields": [],
      "static_args": {},
      "steps": [],
      "tool": "db.truncate_table",
      "window_seconds": null
    },
    {
      "arg_map": {
        "target": "snapshot.current_version"
      },
      "degraded_kind": "irreversible",
      "gates": [
        {
          "check_at": "authorize",
          "description": "The migration must have a down step that has actually been exercised. An unwritten or never-run down migration is not a rollback path.",
          "name": "db_down_migration_exists_and_tested",
          "remediation": "Require down migrations in review and run them in CI. Until then, treat migrations as irreversible and gate them behind a human."
        }
      ],
      "inverse_tool": "db.migrate_down",
      "kind": "compensable",
      "notes": "The only genuinely designed inverse here, and still gated \u2014 because down migrations are routinely unwritten, untested, or destructive. Requiring them in review and running them in CI is what makes this spec true; the spec cannot make it true on its own.",
      "one_shot": false,
      "residue": "A down migration reverses the schema, not the data. Dropping a column the up migration added discards every value written into it, and a down step that recreates a table cannot repopulate it. Rows written under the new schema may not fit the old one at all.",
      "snapshot_fields": [
        "current_version"
      ],
      "static_args": {},
      "steps": [],
      "tool": "db.migrate_up",
      "window_seconds": null
    },
    {
      "arg_map": {
        "name": "args.name",
        "table": "args.table"
      },
      "degraded_kind": "irreversible",
      "gates": [],
      "inverse_tool": "db.drop_index",
      "kind": "reversible",
      "notes": "One of the few cleanly reversible schema operations \u2014 though on a large table both directions can lock or consume enough I/O to be an incident in themselves.",
      "one_shot": false,
      "residue": "",
      "snapshot_fields": [],
      "static_args": {},
      "steps": [],
      "tool": "db.create_index",
      "window_seconds": null
    }
  ]
}